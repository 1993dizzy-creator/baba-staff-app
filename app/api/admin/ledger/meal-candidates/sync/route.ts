import{loadMealCandidateSource,validLedgerSyncMonth}from"@/lib/ledger/employee-costs";import{ledgerJson,requireLedgerActor}from"@/lib/ledger/server";import{supabaseServer}from"@/lib/supabase/server";
export async function POST(request:Request){const auth=await requireLedgerActor();if(auth.response||!auth.actor)return auth.response;const body=await request.json().catch(()=>null)as{month?:unknown}|null;if(!body||!validLedgerSyncMonth(body.month))return ledgerJson({ok:false,code:"INVALID_MONTH"},400);try{const source=await loadMealCandidateSource(body.month);const{data,error}=await supabaseServer.rpc("ledger_sync_candidates_v2",{p_candidate_type:"employee_meal",p_source_type:"attendance_meal_daily",p_rows:source.rows,p_actor_user_id:auth.actor.id});if(error)throw error;const result=data as Record<string,unknown>;if(result.status!=="ok")return ledgerJson({ok:false,code:String(result.status).toUpperCase()},result.status==="forbidden"?403:400);return ledgerJson({ok:true,month:body.month,...result,scannedBusinessDates:source.scannedBusinessDates,ignoredCount:source.ignoredCount,totalAmount:source.totalAmount})}catch(error){console.error("[LEDGER_MEAL_SYNC_FAILED]",error);return ledgerJson({ok:false,code:"MEAL_SYNC_FAILED"},500)}}
