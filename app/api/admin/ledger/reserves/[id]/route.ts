import{ledgerJson,requireLedgerActor}from"@/lib/ledger/server";import{supabaseServer}from"@/lib/supabase/server";export async function PATCH(request:Request,context:{params:Promise<{id:string}>}){const auth=await requireLedgerActor();if(auth.response||!auth.actor)return auth.response;const{id:raw}=await context.params,id=Number(raw),b=await request.json().catch(()=>null)as Record<string,unknown>|null;if(!Number.isSafeInteger(id)||id<1||!b)return ledgerJson({ok:false,code:"INVALID_BODY"},400);try{const{data,error}=await supabaseServer.rpc("ledger_update_reserve_plan_v1",{p_reserve_plan_id:id,p_target_amount:b.targetAmount,p_target_date:b.targetDate||null,p_memo:b.memo||null,p_actor_user_id:auth.actor.id});if(error)throw error;const result=data as{status?:string};return result.status==="updated"?ledgerJson({ok:true,result}):ledgerJson({ok:false,code:String(result.status).toUpperCase()},result.status==="forbidden"?403:400)}catch(error){console.error("[LEDGER_RESERVE_UPDATE_FAILED]",error);return ledgerJson({ok:false,code:"RESERVE_UPDATE_FAILED"},500)}}
