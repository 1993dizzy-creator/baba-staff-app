import {supabaseServer} from "@/lib/supabase/server";import {payrollJson,requirePayrollActor} from "@/lib/payroll/server";
const additions=new Set(["incentive","meal","transport","housing","other_addition"]);const deductions=new Set(["insurance_tax","advance","penalty","other_deduction"]);type Context={params:Promise<{runId:string;employeeId:string}>};
async function mutate(request:Request,context:Context,operation:"create"|"update"|"delete"){const auth=await requirePayrollActor();if(auth.response||!auth.actor)return auth.response;const params=await context.params;const runId=Number(params.runId),employeeId=Number(params.employeeId);const body=await request.json().catch(()=>null) as Record<string,unknown>|null;const amount=Number(body?.amount??0),category=String(body?.category??""),direction=String(body?.direction??"");const validPair=(direction==="addition"&&additions.has(category))||(direction==="deduction"&&deductions.has(category));if(!Number.isSafeInteger(runId)||!Number.isSafeInteger(employeeId)||!body||(operation!=="delete"&&(!validPair||!Number.isSafeInteger(amount)||amount<0||typeof body.description!=="string"||!body.description.trim()||typeof body.reason!=="string"||!body.reason.trim()))||(operation==="delete"&&(!Number.isSafeInteger(Number(body.itemId))||typeof body.reason!=="string"||!body.reason.trim())))return payrollJson({ok:false,code:"INVALID_ITEM",message:"급여 항목과 사유를 다시 확인해주세요."},400);const{data,error}=await supabaseServer.rpc("payroll_mutate_item_v3",{p_run_id:runId,p_run_employee_id:employeeId,p_item_id:body.itemId??null,p_operation:operation,p_category:category||"other_addition",p_direction:direction||"addition",p_amount:amount,p_description:body.description??"",p_reason:body.reason,p_actor_user_id:auth.actor.id});if(error){const status=error.message.includes("LOCKED")?409:error.message.includes("MISMATCH")||error.message.includes("NOT_FOUND")?404:500;return payrollJson({ok:false,code:status===409?"RUN_LOCKED":"ITEM_MUTATION_FAILED",message:status===409?"작성 중인 급여만 변경할 수 있습니다.":"급여 항목을 변경하지 못했습니다."},status)}const itemId=Number(data);return Number.isSafeInteger(itemId)&&itemId>0?payrollJson({ok:true,itemId}):payrollJson({ok:false,code:"ITEM_MUTATION_FAILED",message:"급여 항목을 변경하지 못했습니다."},500)}
export function POST(request:Request,context:Context){return mutate(request,context,"create")}export function PATCH(request:Request,context:Context){return mutate(request,context,"update")}export function DELETE(request:Request,context:Context){return mutate(request,context,"delete")}
