const ZERO=BigInt(0),THOUSAND=BigInt(1000),MILLION=BigInt(1000000);
const milli=(value:string|number)=>{const negative=String(value).startsWith("-"),raw=negative?String(value).slice(1):String(value),[whole,fraction=""]=raw.split("."),units=BigInt(whole||"0")*THOUSAND+BigInt((fraction+"000").slice(0,3));return negative?-units:units};
const decimal=(value:bigint)=>`${value<ZERO?"-":""}${(value<ZERO?-value:value)/THOUSAND}.${String((value<ZERO?-value:value)%THOUSAND).padStart(3,"0")}`;
export function allocateOwnerPool(pool:string|number,lines:Array<{participantId:number;rate:string|number;sortOrder:number}>){const total=milli(pool),ordered=[...lines].sort((a,b)=>a.sortOrder-b.sortOrder||a.participantId-b.participantId);let used=ZERO;return ordered.map((line,index)=>{const rateParts=String(line.rate).split("."),rate=BigInt(rateParts[0]||"0")*MILLION+BigInt(((rateParts[1]??"")+"000000").slice(0,6)),assigned=index===ordered.length-1?total-used:total*rate/MILLION;used+=assigned;return{...line,assignedAmount:decimal(assigned)}})}
