export type BepReferenceMonth={month:string;revenue:number;variableCost:number};
export function calculateBep(input:{referenceMonths:BepReferenceMonth[];fixedCost:number;currentSales:number;remainingBusinessDays:number}){const usable=input.referenceMonths.filter(row=>row.revenue>0).slice(-3),variableCostRatio=usable.length?usable.reduce((sum,row)=>sum+row.variableCost/row.revenue,0)/usable.length:null,contributionMarginRatio=variableCostRatio===null?null:1-variableCostRatio,unavailableReason=usable.length===0?"INSUFFICIENT_REVENUE":contributionMarginRatio===null||contributionMarginRatio<=0?"NON_POSITIVE_CONTRIBUTION_MARGIN":null,bepRevenue=unavailableReason?null:input.fixedCost/(contributionMarginRatio as number),gap=bepRevenue===null?null:Math.max(0,bepRevenue-input.currentSales),exceeded=bepRevenue===null?null:Math.max(0,input.currentSales-bepRevenue),requiredDailySales=gap===null||input.remainingBusinessDays<=0?null:gap/input.remainingBusinessDays;return{referenceMonths:usable.map(row=>({...row,variableCostRatio:row.variableCost/row.revenue})),variableCostRatio,contributionMarginRatio,bepRevenue,gap,exceeded,requiredDailySales,unavailableReason}}
